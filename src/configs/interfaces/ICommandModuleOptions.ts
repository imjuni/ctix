export interface ICommandModuleOptions {
  /**
   * Output directory. Default value is same project directory
   *
   * @required
   * @command build
   * @mode bundle, module
   */
  output: string;
}
